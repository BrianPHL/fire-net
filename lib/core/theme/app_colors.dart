import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background colors
  static const Color background = Color(0xFF0A0E1A);
  static const Color cardBackground = Color(0xFF1A1F2E);
  static const Color cardBackgroundLight = Color(0xFF232938);

  // Primary colors
  static const Color primary = Color(0xFF00D9A3);
  static const Color primaryDark = Color(0xFF00B887);

  // Status colors
  static const Color danger = Color(0xFFFF5757);
  static const Color dangerBackground = Color(0xFF2D1F1F);
  static const Color warning = Color(0xFFFFB800);
  static const Color warningBackground = Color(0xFF2D2A1F);
  static const Color safe = Color(0xFF00D9A3);
  static const Color safeBackground = Color(0xFF1F2D28);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textTertiary = Color(0xFF6B7280);

  // Sensor indicator colors
  static const Color temperatureColor = Color(0xFFFF5757);
  static const Color smokeColor = Color(0xFFFF5757);
  static const Color gasColor = Color(0xFF00D9A3);

  // Chart colors
  static const Color chartLine = Color(0xFF00D9A3);
  static const Color chartGrid = Color(0xFF2D3648);

  // Badge colors
  static const Color activeEmergency = Color(0xFFFF5757);
  static const Color resolvedStatus = Color(0xFF00D9A3);

  // Border colors
  static const Color borderColor = Color(0xFF2D3648);
  static const Color dividerColor = Color(0xFF1F2937);
}
